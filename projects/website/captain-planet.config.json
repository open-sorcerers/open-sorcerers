{
  "offline": {
    "description": "sometimes you wanna use local servers, like, only locally",
    "env": ["OFFLINE"]
  },
  "environment": {
    "description": "the environment! sometimes it's production -- watch out",
    "env": ["NODE_ENV"]
  }
}
