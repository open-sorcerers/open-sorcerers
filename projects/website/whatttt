import { Box } from 'rebass'
import { Z_INDEX } from '@styles/constants'
import styled from '@emotion/styled'
import { css } from '@emotion/core'
import { Link } from 'gatsby'
import { minBreak, maxBreak } from '@styles/media'
import { primary } from '@styles/colors'
import { transitionEaseIn } from '@styles/animation'

export const activeNav = css`
  padding-right: 0;
  padding-left: 0;
`
export const inactiveNav = css`
  padding: 0;
  ${minBreak.S(`
    border-left: 12vw solid ${primary};
    margin-left: -12vw;
  `)}
`

export const StyledNavigation = styled(Box)`
  z-index: ${Z_INDEX.INTERACTIVE};
  text-align: center;
  display: flex;
  align-items: center;
  background-color: #409;
  color: #fff;
  height: 14rem;
  padding-top: 2rem;
  padding-bottom: 2rem;
  ${minBreak.S(`
     height: 5rem; 
  `)}

  ${transitionEaseIn('0.6s', ['background', 'padding', 'border'])};
`

export const Inner = styled(Box)`
  display: flex;
  flex-flow: nowrap row;

  ${maxBreak.S(`
    flex-flow: nowrap column;
  `)}
`

export const Brand = styled(Link)`
  display: flex;
  align-items: center;
  flex: 0 0 auto;
  color: #fff;
  font-size: 20px;
  line-height: 24px;
  text-decoration: none;
  text-transform: uppercase;
  font-weight: 900;

  svg {
    width: 100%;
    fill: white;
    max-height: 50px;
  }

  ${maxBreak.S(`
    margin: 8px 0;
    justify-content: center;
  `)}

  :hover {
    text-decoration: none;
  }
`

export const Nav = styled(Box)`
  display: flex;
  flex-flow: nowrap row;
  flex: 1 1 auto;
  align-items: center;

  ${maxBreak.S(`
    margin: 0 -24px;
    justify-content: center;
    overflow-x: auto;
  `)}
`

export const Item = styled(Box)`
  padding: 16px 8px;
  color: #fff;
  font-weight: 500;
  line-height: 24px;
  opacity: ${p => (p.isActive ? '1' : '0.6')};
  text-decoration: none;

  :hover {
    opacity: 1;
    text-decoration: none;
  }
  ${maxBreak.S(`
    padding: 8px;
  `)}
`

export const MenuWrapper = styled.div`
  text-align: center;
  margin: 0;
  display: flex;
  flex-flow: nowrap row;
  align-items: center;
`
