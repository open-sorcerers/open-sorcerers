---
path: /example/diff
title: diff example 
author: brekk
---
import {Diff} from '@components/Diff'
import { Auth } from '@services/auth'


<Diff content={
[
`
  const user = JSON.parse('${JSON.stringify(Auth().getUser())}')
  const wiggle = "~~~~"
  // COMMENT
  const render = input => Array(input)
  .fill('x')
  .map(
    (x, i) => Array(i).fill(x).join('')
  ).join(' -> ')
  .replace(/> x -/g, wiggle)
  // WHATEVER
`
  ,
`
  const wiggle = "~~~~"
  // COMMENT
  import {always as K, pipe, range, map, addIndex, join, replace} from 'ramda'
  const addX = map(K('x'))
  const innerFill = (x, i) => pipe(
    range(0),
    addX
  )(i)
  const render = pipe(
    range(0),
    addX,
    addIndex(map)(innerFill),
    join(' -> '),
    replace(/> x -/g, wiggle)
  )
`
]
} />

That's cool!
