---
title: Function | Deterministic
path: /glossary/deterministic
author: brekk
---

## Deterministic

A function which will always return the same output given the same input.

```js
const identity = x => x
```
