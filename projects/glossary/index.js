module.exports = {
  arity: "Arity indicates the number of arguments a function should take.",
  curry:
    "The technique of translating the evaluation of a function that takes multiple arguments into evaluating a sequence of functions, each with a single argument.",
  "side-effect":
    "When a function makes change outside of its local scope, it is said to have side-effects.",
  pure:
    "A pure function does not manipulate anything outside of its local context, it has no side-effects and behaves with idempotence.",
  composition:
    "An approach for sequencing the operation of several unary functions into a single unary pipeline."
}
