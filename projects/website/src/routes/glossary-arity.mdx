---
title: Arity
path: /glossary/arity
author: brekk
---

## Arity

The number of parameters a function takes.

```js
const nullary = () => {}
const unary = x => x
const binary = (a, b) => [a, b]
const ternary = = (a, b, c, d) => ({a, b, c, d})
```
