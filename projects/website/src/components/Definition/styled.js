import styled from '@emotion/styled'
import { Box } from 'rebass'

export const StyledDefinition = styled(Box)`
  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    a {
      font-family: obviously-narrow, 'Obviously', sans-serif;
      font-weight: 500;
      text-transform: uppercase;
      font-style: italic;
    }
  }
`
