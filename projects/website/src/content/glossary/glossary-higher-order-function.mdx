---
datePublished: 2020-01-18
dateEdited: 2020-01-26
menu: learn
title: Higher-Order Function
path: /glossary/higher-order-function
---

import Def from '@components/Definition'

<Def of="Higher-Order Function" parent="Function" />

A function which takes a function and possible additional parameters, and returns a function.

```js
const map = fn => xs => xs.map(x => fn(x))
// import {map} from 'ramda'
map(times(2))([1,2,3]) === [2,4,6]
```
