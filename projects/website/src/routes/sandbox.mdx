---
path: /this-is-dope
title: this is a dope sandbox, delete it later
author: your mom dot com
---

import {Diff} from '@components/Diff'

<Diff content={[
  `
  // COMMENT
const render = input => Array(input)
.fill('x')
.map(
  (x, i) => Array(i).fill(x).join('')
).join(' -> ')
.replace(/> x -/g, '~~~~')
// WHATEVER
`,
`
// COMMENT
import {always as K, pipe, range, map, addIndex, join, replace} from 'ramda'
const addX = map(K('x'))
const innerFill = (x, i) => pipe(
  range(0),
  addX
)(i)
const render = pipe(
  range(0),
  addX,
  addIndex(map)(innerFill),
  join(' -> '),
  replace(/> x -/g, '~~~~')
)
`
]} />

That's cool!
